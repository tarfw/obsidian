{
  "name": "tarharness",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "npm run types && tsc --noEmit",
    "test": "vitest run",
    "verify": "npm run check && npm run test && wrangler deploy --dry-run"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15",
    "effect": "4.0.0-rc.112",
    "jose": "^6.2.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260905.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.129.0"
  }
}
